services:
  node-red:
    image: nodered/node-red:latest
    container_name: node-red
    ports:
      - "1880:1880"
    user: "1000:1000"
    volumes:
      - ./node-red/data:/data
    restart: always
